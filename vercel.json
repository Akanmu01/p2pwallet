{
    "builds": [{
        "src": "p2pwallet/wsgi.py",
        "use": "@ardnt/vercel-python-wsgi",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "p2pwallet/wsgi.py"
        }
    ]
}
